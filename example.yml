---
platform: linux

image_resource:
  type: registry-image
  source: {repository: issho/concourse-buildkit}

params:
  REPOSITORY: concourse/builder-task
  TAG: latest
  CONTEXT: builder-task

inputs:
- name: builder-task

outputs:
- name: image

caches:
- path: cache

run: {path: build}
